<%= form_for @yo, validate: true do |f| %>
  <% if @yo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@yo.errors.count, "error") %> prohibited this yo from being saved:</h2>

      <ul>
      <% @yo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
